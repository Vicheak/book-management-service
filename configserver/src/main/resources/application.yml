server:
  port: 8071

spring:
  application:
    name: configserver

  profiles:
    active: git

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Vicheak/book-config-server-private
          username: Vicheak
          password: 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
          default-label: main

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

encrypt:
  key: bookservice@123

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      probes:
        enabled: true
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
  info:
    env:
      enabled: true

info:
  app:
    name: configserver
    description: provide configuration service
    version: 1.0.0

logging:
  level:
    com.vicheak.book.configserver: DEBUG
  pattern:
    level: "%5p [${spring.application.name},%X{trace_id},%X{span_id}]"